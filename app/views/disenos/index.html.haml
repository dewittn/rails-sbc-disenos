-javascripts "disenos_index"
-page_title t('design.index.title')
.search
  %h1= yield :title
  = render :partial => 'letters'
#result= render :partial => 'diseno', :collection => @disenos
- button_2(t('design.index.new'),new_diseno_path)
