#top-bar
  .float-left
    = content_for :top_link
    |
    - unless @letters.nil? 
      = render :partial => 'disenos/letters'
  .center-text
    = content_for :app_title
    \:
    = content_for(:title) || defualt_title
  .float-right= yield :logout
  .clear