#top-bar
  .float-left
    = yield :top_link
    |
    - unless @letters.nil? 
      = render :partial => 'disenos/letters'
  .center-text
    = yield :app_title
    \:
    = yield(:title) || defualt_title
  .float-right= yield :logout
  .clear